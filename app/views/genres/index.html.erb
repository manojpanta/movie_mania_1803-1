<% @genres.each do |g| %>
<%= link_to g.name, genre_path(g)%>
<%end%>
<% if current_admin?%>
  <h1>Create New Genre</h1>
  <%= form_for @genre do |f| %>
    <%= f.label :name%>
    <%= f.text_field :name%>

    <%=f.submit%>
  <%end%>
<%end%>
